document.addEventListener('DOMContentLoaded', startup, false);

var startup = function() {

	var zones = document.querySelector('div.zones');

	zones.forEach(function(zoneElement, index) {
		
	});

};

var Zone = function() {
};

Zone.prototype.
